name: Sync

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror the Github organization repos to Gitee.
        uses: Yikun/hub-mirror-action@master
        with:          
          # 表示需要被同步的源端账户名，即 Github 的账户名
          src: 'github/liqycs'
          # 表示需要同步到的目的端账户名，即 Gitee 的账户名
          dst: 'gitee/liqycs'
          # 注意在 Settings->Secrets 配置私钥 GITEE_RSA_PRIVATE_KEY
          dst_key: ${{ secrets.GITEE_RSA_PRIVATE_KEY }}
          # Gitee TOKEN
          dst_token:  ${{ secrets.GITEE_TOKEN }}
          # 表示单一仓库同步
          static_list: "liqycs"
          # 表示启用 git push -f 强制同步
          force_update: true
          # 表示启用 debug 开关，会显示所有执行命令
          debug: true

      - name: Build Gitee Pages
        uses: yanglbme/gitee-pages-action@main
        with:
          # 注意替换为你的 Gitee 用户名
          gitee-username: liqycs
          # 注意在 Settings->Secrets 配置 GITEE_PASSWORD
          gitee-password: ${{ secrets.GITEE_PASSWORD }}
          # 注意替换为你的 Gitee 仓库，仓库名严格区分大小写，请准确填写，否则会出错
          gitee-repo: liqycs/liqycs
          # 要部署的分支，默认是 master，若是其他分支，则需要指定（指定的分支必须存在）
          branch: master
          # 要部署的分支上的目录
          directory: docs